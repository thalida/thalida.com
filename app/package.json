{
  "name": "thalida-app",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "vite preview --host --outDir dist --port 4321"
  },
  "dependencies": {
    "astro": "^5.10.1",
    "yaml": "^2.8.2"
  }
}
