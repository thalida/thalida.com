---
import { prettifySlug } from "@lib/format-utils";

interface Props {
  subcategory?: string;
}

const { subcategory } = Astro.props;
---

{subcategory && <p class="m-0 font-body text-muted text-2xs tracking-wide">{prettifySlug(subcategory)}</p>}
