---
import { categoryDisplay } from "@lib/format-utils";

interface Props {
  collection: string;
  categories: string[];
  activeCategory?: string;
}

const { collection, categories, activeCategory } = Astro.props;
---

{
  categories.length > 0 && (
    <div class="mb-6">
      <div class="flex flex-wrap gap-2">
        <a
          href={`/${collection}`}
          class:list={[
            "border font-body transition-colors no-underline capitalize text-xs rounded-sm py-1 px-3 tracking-wide",
            !activeCategory
              ? "bg-white/15 text-white! border-white"
              : "bg-transparent border-teal text-teal hover:bg-teal/10 hover:text-teal",
          ]}
        >
          All
        </a>
        {categories.map((cat) => (
          <a
            href={`/${collection}/${encodeURIComponent(cat)}`}
            class:list={[
              "border font-body transition-colors no-underline capitalize text-xs rounded-sm py-1 px-3 tracking-wide",
              cat === "dead-links"
                ? activeCategory === cat
                  ? "bg-error/20 text-error! border-error"
                  : "bg-transparent border-error/50 text-error/70 hover:bg-error/10 hover:text-error"
                : activeCategory === cat
                  ? "bg-white/15 text-white! border-white"
                  : "bg-transparent border-teal text-teal hover:bg-teal/10 hover:text-teal",
            ]}
          >
            {categoryDisplay(cat)}
          </a>
        ))}
      </div>
    </div>
  )
}
