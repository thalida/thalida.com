---
const wsUrl = import.meta.env.PUBLIC_CHAT_WS_URL || "ws://localhost:8787/ws";
---

<aside id="chat-panel" transition:persist="chat">
  <div id="chat-header">
    <h2>Chat</h2>
    <a id="chat-admin-link" href="/login">admin login</a>
  </div>
  <div>
    <span id="chat-owner-status">offline</span>
    &middot;
    <span id="chat-user-count">0 users connected</span>
  </div>
  <hr />

  <div id="chat-room">
    <div id="chat-messages" style="min-height:200px;max-height:60vh;overflow-y:auto;"></div>
    <div id="chat-username-bar">
      <span id="chat-rename-controls">
        <label for="chat-username">chatting as:</label>
        <input
          id="chat-username"
          type="text"
          autocomplete="off"
          minlength="2"
          maxlength="20"
          pattern="[a-z0-9_.-]+"
          title="2-20 characters: lowercase letters, numbers, hyphens, underscores, dots. No spaces."
        />
        <button id="chat-rename-btn" type="button">save</button>
      </span>
      <span id="chat-context" hidden>
        about
        <select id="chat-context-select">
          <option value="page"></option>
          <option value="general">general</option>
        </select>
      </span>
    </div>
    <div>
      <input id="chat-input" type="text" placeholder="Type a message..." autocomplete="off" />
      <button id="chat-send" type="button">Send</button>
    </div>
  </div>
</aside>

<meta name="chat-ws-url" content={wsUrl} />

<script src="./chat-client.ts"></script>

<style>
  @import "./Chat.css";
</style>
